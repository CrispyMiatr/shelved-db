export interface ProductCardType {
    name: string;
    brand?: string;
    volume: number;
    country: string;
    img: string;
}

export interface BrandCardType {
    brand: string;
    count: number;
    img: string;
}